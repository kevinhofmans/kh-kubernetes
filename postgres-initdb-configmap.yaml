apiVersion: v1
#M.b.v. configmap kan je instellingen wijzigen zonder een nieuwe container image te moeten bouwen.

kind: ConfigMap
metadata:
  name: kh-postgres-initdb
data:
  init.sql: |
    CREATE TABLE users (
        id SERIAL PRIMARY KEY,
        name VARCHAR(100) NOT NULL
    );

    INSERT INTO users (name)
    VALUES ('Captain Docker');